export const metadata = {
  title: 'Services',
  description: 'Growth diagnostics, acquisition strategy, UX, and retention. Fixed-fee engagements for growth-stage companies whose marketing has stalled.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
