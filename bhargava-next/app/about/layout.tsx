export const metadata = {
  title: 'About',
  description: 'Seven years across performance marketing, brand strategy, and product. Based in Mumbai. I work at the intersection of strategy and code.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
