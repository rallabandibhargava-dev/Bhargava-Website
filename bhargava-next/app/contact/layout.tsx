export const metadata = {
  title: 'Contact',
  description: 'Tell me about the problem. One of two Q3 slots open. Response within 48 hours.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
